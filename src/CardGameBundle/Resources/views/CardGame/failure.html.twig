Le code du statut :{{ statusCode }} : Ops, votre solution est erronée (Elle ne respecte pas les ordres de tri ci-dessous) :
<br>
Order de tri par catégorie
{{ dump(categoryOrder) }}
Order de tri par valeur
{{ dump(valueOrder) }}
Votre solution :

{{ dump(cards) }}

Solution correcte est la suivante :

{{ dump(message) }}
